package de.mmerlin.mmxp.whatabubble.config;

public enum BubbleVisibility {
    /** Show speech bubbles for every player (including yourself). */
    ALL,
    /** Show speech bubbles for other players only, not for yourself. */
    OTHERS_ONLY,
    /** Hide all speech bubbles. */
    NONE
}

